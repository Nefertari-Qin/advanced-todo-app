package ui;

import model.AccountManager;

public class AccountFrame {
    private AccountManager accountManager;

    public AccountFrame(AccountManager accountManager){
        this.accountManager = accountManager;
    }
}
